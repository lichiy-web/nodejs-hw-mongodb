description:
  Successfully updated a contact! Response contains the json object with data of
  the updated contact.
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 200
        message:
          type: string
          example: Successfully updated a contact!
        data:
          type: object
          $ref: ../schemas/contact.yaml
